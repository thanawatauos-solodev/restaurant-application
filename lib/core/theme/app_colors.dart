import 'package:flutter/material.dart';

class AppColors {
  // 🟢 Primary
  static final emeraldGreen =
      HSLColor.fromAHSL(1.0, 165, 0.45, 0.25).toColor();

  static final champagneGold =
      HSLColor.fromAHSL(1.0, 45, 0.30, 0.92).toColor();

  static final deepCharcoal =
      HSLColor.fromAHSL(1.0, 165, 0.40, 0.12).toColor();

  static final amberCleaning =
      HSLColor.fromAHSL(0.5, 45, 1.0, 0.50).toColor();

  // ⚪️ Background & Surface
  static final softWhite =
      HSLColor.fromAHSL(1.0, 160, 0.20, 0.98).toColor();

  static final cardWhite =
      HSLColor.fromAHSL(1.0, 0, 0.0, 1.0).toColor();

  static final borderColor =
      HSLColor.fromAHSL(1.0, 160, 0.15, 0.88).toColor();

  // 🌙 Dark Mode
  static final darkBackground =
      HSLColor.fromAHSL(1.0, 165, 0.40, 0.08).toColor();
}
